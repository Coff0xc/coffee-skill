# Usage API Fixture

Run the fast inner loop:

```bash
node test/usage.test.js
```

The helper normalizes account usage events and calculates remaining budget for starter, team, and enterprise plans.
